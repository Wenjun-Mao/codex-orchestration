# Repository workflow

After each implementation round, run proportional checks, commit the round's changes,
and push to the configured upstream before reporting completion. Exclude unrelated
user changes; report any commit/push blocker rather than force-pushing or rewriting history.
