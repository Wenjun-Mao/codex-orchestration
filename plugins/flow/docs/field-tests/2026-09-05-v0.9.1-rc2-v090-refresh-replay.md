# v0.9.1-rc.2 exact-v0.9.0 refresh replay

Date: 2026-09-05

Candidate: annotated `v0.9.1-rc.2` at
`8967b7c9df4abf6114db58034d6171dedcae45d8`

## Purpose

A real closed v0.9.0 run exposed that the refresh target still required the
v0.8 cleanup-plan field names during source namespace retirement. This replay
tests the exact failed lifecycle segment after the version-bound transfer fix
and an App reload onto RC2.

## Authority

- Installed package and plugin version: `0.9.1-rc.2`
- Installed runtime bundle:
  `a634c8e520cc3bb69bb2595fe1570215f1ccfa86f5ec291094dd0cd696e1b946`
- Loaded refresh skill SHA-256:
  `bd413fac0f4102e57c891b533725a744903bd06d823e565d8ed92a58f981db57`
- RC2 artifact SHA-256:
  `b8396de5552d2da6a94023294180b7174563d1acbe8db65fd3b698fadfcf5eb5`
- Marketplace and installed-cache normalized digest:
  `49fa357be0b26e530ae454f262d1277ed66c30c94b8df21ce2b48ca065df0698`

The post-restart authority inspection returned `fresh` with
`mutation_performed=false` and the exact package, plugin, bundle, and skill
identities above.

## Replay

The regression extracted immutable tag `v0.9.0`, activated a real source run
through that source package, completed and disposed its bounded native
subagent, produced a current terminal-ready audit, and closed the run. The
source runtime emitted `codex-flow-v09-cleanup-plan` with
`blocking_launch_ids` and no legacy `blocking_operation_ids` field.

The RC2 target then completed `refresh inspect`, `refresh prepare`, and
`refresh apply` with no decisions, replacements, target workflow, or archive
evidence. Apply recorded `already-terminal` / `closed`, returned
`consumed-clean-start`, and removed both the v0.9.0 source namespace and the
refresh handoff namespace. The focused replay passed after App reload, and the
same case passed inside both full 101-test RC2 runs.

## Boundary

This evidence accepts cleanup-complete terminal v0.9 source retirement only
for a no-replacement refresh. Refresh handoff v1 still names legacy v0.8
creation-operation discard authority. RC2 deliberately rejects any attempt to
represent a v0.9 launch-based discard with that field; no universal predecessor
or launch-discard support is claimed.
