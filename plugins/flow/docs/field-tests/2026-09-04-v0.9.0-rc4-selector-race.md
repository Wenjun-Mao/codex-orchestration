# v0.9.0-rc.4 selector-race canary

Date: 2026-09-04
Run: `v09-rc4-selector-race-canary-20260904`
Task: `v09-rc4-selector-race-executor`
Launch: `task-launch-v1-23677306132e2a0edcd8d073f466595017f8aceb701eb2e1716adfecf5a78ee6`

## Field-test result

The executor launch activated successfully on branch
`codex/v09-rc4-selector-race-canary` from baseline revision
`3060c93fcdb41cb87117ecc92ffb4e19e3cc2528`. At first-turn callback time,
accepted and observed selector evidence were unavailable. The executor-visible
selector state was therefore recorded as `accepted: null` and `observed: null`;
no selector value was inferred or invented.

The requested selector authority was model `gpt-5.6-luna` with `medium`
reasoning effort. The later coordinator/App reconciliation may record a
provisional accepted result, but that enrichment must preserve this immutable
receipt and remain valid when it matches the original request.

## Authority and scope

- The executor start claim and Git activation authenticate this task and its
  reserved worktree.
- This note records only the executor-side first-turn observation; it does not
  assert a later App result that was unavailable to this executor.
- Callback admission must accept the null accepted/observed fields when the
  configured and requested selectors match launch authority.
- Later matching App reconciliation must leave the result dispositionable;
  contradictory or invented non-null terminal evidence remains invalid.

## Exit condition

This slice passes when the callback is delivered with the null selector
evidence recorded above, the coordinator later enriches launch authority with
the matching provisional App result, and disposition completes without receipt
correction, retry, or a mutable evidence state machine.
