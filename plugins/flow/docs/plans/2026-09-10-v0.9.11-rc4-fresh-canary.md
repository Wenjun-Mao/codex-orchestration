# RC4 fresh mixed-lifecycle canary

Status: Approved under the user's bounded RC4 recovery and replacement-canary extension.

## Outcome and authority

Prove a fresh RC4 ordinary delivery from generated assignment through dependent work, reporting, child-first cleanup, director acceptance, and useful successor work. This is the installed gate for the controlling assignment-boundary consolidation plan, not another implementation checkpoint.

Use only saved project 62e65e92-e972-4a07-804e-c356da754cf1 at /Users/wjmao/projects/utility_projects/codex-flow-v0.9.11-canary. RC3 was failed/abandoned, cancelled and unplugged with zero residue. Keep its A/B files unchanged on main at fac07b472f06dd61ffba429fe591d1b0516d7c29. Failure evidence survives outside Flow state.

Installed candidate: 0.9.11-rc.4+codex.20260910160550; package/runtime 0.9.11-rc.4; bundle ac06117322aa508ac04bceb5aaa436e8b66a974c71759c8ca94e8f93e10eac41. App observed at 26.903.71938 build 8576. Authenticate loaded authority; no package/source changes or installations.

## Bounded work and preservation

One fresh Sol-high coordinator, one dependent visible Luna-xhigh executor, then one director-dispatched successor coordinator. The implementation coordinator remains a sibling and does not own these tasks.

After the one refresh inspection, authenticate the actual App-assigned linked checkout and attach the unused local branch codex/v0911-rc4-canary-coordinator before capturing run repository authority. Do not activate on a detached checkout. Use that delivery branch for serial integration; main remains the separate preservation checkout. Preflight branch collision and clean baseline; never move a branch from another checkout.

Represent A as coordinator-work and B as task-thread, with B depending on completed A. Reserve the exact executor branch codex/v0911-rc4-canary-executor before run activation. Derive/check path and resource reservations through existing commands. Owned files: canary/rc4-artifact-a.txt and canary/rc4-artifact-b.txt; resource v0911-rc4-fresh-chain. No other source changes.

A writes and commits a unique RC4 marker in rc4-artifact-a.txt through workflow local start/complete. B must read those accepted committed A bytes in its starting baseline and produce rc4-artifact-b.txt containing a provenance prefix plus the exact A content; commit and persist its terminal receipt. The executor selector is Luna-xhigh because this is mechanical file transformation with exact-byte verification. No subagents or extra tasks.

Use normal full first-turn executor contract and pinned task start. Record one launch attempt and one native creation with explicit selectors. Preserve the actual returned host ordering; never manufacture provisional evidence or rewrite a provisional result as ready. Reconcile an equivalent original creation result without changing original timestamps/provenance where applicable; observe start-derived ready membership through public status.

Integrate B and verify exact A/B bytes and ownership. Ordinary fast-forward of the clean disposable primary main to the verified delivery tip is authorized for preservation; never reset, force, cherry-pick/squash, or alter another repository. This is not permission to change a registered activation binding.

## Completion and evidence

Use public callback, disposition, integration, verification, owning-host child archive/reconciliation, cleanup, run audit and normal close. Fresh archived/no-active evidence is required; no archive replay or manual worktree removal. The executor worktree/branch must be gone and the audit must have zero unresolved fences. Keep assignment reporting active for the final.

Return actual creation/start/archive counts; configured/requested/accepted/observed selectors; actual ordering and first-turn result; committed A/B and verification; child cleanup and run closure. Separate App provisioning time from Flow activation time when observable; do not infer timings. Use one complete idle final through the exact registered route, with explicit manual reporting only if the supported hook path is unavailable and no submission is ambiguous.

Director reviews delivered evidence, accepts and archives/reclaims the coordinator through public owning-host closeout, then proves locator retirement. Only afterward the director prepares and dispatches the successor in this same saved project. The successor must read and verify exact A/B bytes and perform a bounded useful verification/result under fresh public assignment/run authority. It must not be created by this coordinator.

## Stops and exclusions

No product code, release promotion, full-suite rerun, model benchmark, new project, pilot work, AGENTS.md access, historical-state repair, or extra canary. Preserve and report any unsupported boundary, identity contradiction, ambiguous host result, or failed acceptance gate. Do not convert a recovered run into an uninterrupted pass or patch the candidate during this gate.

## Accepted live result — September 10

The director accepted the fresh RC4 mixed lifecycle and the useful successor as the
complete installed gate for stable promotion. The accepted evidence preserves A at
`e34e386e35c246f3b1f3d99e303398b93e7a5159` with SHA-256
`1fe0719baf79a4e67a8af2748b4ba6987f21391075e4e0825cf60e44389bf334`
and B at `1fa9d86cd8803231cc79c030ee0cdbc84c6ca2fd` with SHA-256
`acc6cdff819774840ff44108db8a7b1930467eb0f9f7cee2ed84f7d428f31011`.
B contains its provenance prefix followed by the exact A bytes. Executor start and
Git activation preceded provisional reconciliation; the original creation and
selector timestamps were retained and the observed selector remained unavailable,
while requested and accepted authority stayed Luna-xhigh.

The Sol-high coordinator, Luna-xhigh executor, and director-dispatched Terra-high
successor all completed their bounded work and were archived and reclaimed. Both
runs closed normally with zero unresolved fences, both assignment results were
accepted and retired, all report locators were retired, and the disposable project
ended with only its clean `main` worktree at `1fa9d86cd8803231cc79c030ee0cdbc84c6ca2fd`.
The separate RC3 recovery remains classified as a failed canary recovery, with its
preserved A/B evidence; it is not part of the successful RC4 claim.

Two validation-only invocations were corrected before fresh-run admission. The
first refresh inspection passed `coordinate/SKILL.md` where the installed refresh
contract required `refresh/SKILL.md`. The first activation also tried to reserve
the already-attached coordinator branch as a branch fence, while RC4 treats that
attached branch as repository authority and permits only the future executor branch
to be fenced. Both attempts were rejected without creating run or host state.

During director closeout, the first acceptance attempt rejected an expired idle
observation. A fresh actual idle observation was supplied and acceptance completed
with one archive call. No admitted-run repair, archive replay, manual journal edit,
or historical-state repair occurred.
