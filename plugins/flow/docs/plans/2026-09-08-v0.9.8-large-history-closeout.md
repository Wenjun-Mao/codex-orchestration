# v0.9.8 large-history closeout

Status: Approved by the user on 2026-09-08.

## Outcome

Allow exact archived tasks with large histories to complete Flow closeout
without loading their entire transcripts into memory. Finish the accepted
v0.9.7 release assignment's pending retirement, and separately retire the
failed RC15 canary honestly through supported authority.

Primary outcome: safe completion of existing cleanup, not another test harness.
Causal question: can the archive adapter authenticate the existing large
archived session with bounded memory while preserving its identity, integrity,
and no-active-session checks?
Cheapest safe direct attempt: run the corrected read-only observer against the
existing 96,032,235-byte archived coordinator, then resume its exact closeout.

## Starting evidence and scope

- Accepted stable v0.9.7 is tagged at
  `9604808667f8b5be06c23d75c5daf475ca293912`. Authenticate the current checkout,
  upstream, and dirty set before work; preserve post-tag documentation.
- Release coordinator `01a07994-389b-79b3-a471-00b14588f8a8` is already archived.
  Its worktree is absent; assignment
  `coordinator-assignment-v1-a562e0624ee0c36a7aab8cdfb9aaa2062933b4f8f8f897a9c3163300f5bcedab`
  is accepted but closeout-pending. Its branch and route retirement remain.
- The private archive observer rejects the regular archived session at its
  32 MiB whole-file cap. It currently reads, splits, and parses the full file.
  ADR 0042 records a related streaming lesson; it is context, not authority to
  restore historical executable compatibility or its active-file semantics.
- RC15 coordinator `01a07de9-0834-7701-a5ce-cce83a56abf5` and its retained
  immutable run are separate failed evidence. Its child was integrated and
  removed; the coordinator's retirement must not fabricate normal completion.

Non-goals: routing changes, dashboards, automatic saved-project removal,
permission-setting changes, messaging redesign, predecessor migration,
general cleanup sweeps, new canary projects, or new compatibility frameworks.
Do not touch AGENTS.md, pilot repositories, unrelated tasks, or remote refs.

## Consequential decisions

1. Keep the correction inside the Codex App archive adapter. Stream the exact
   file with incremental hashing and bounded parsing; retain only required
   metadata and counters. Preserve the existing evidence format and digest
   meaning where possible. Do not merely raise the cap while retaining the
   entire transcript, sample a tail, or accept identity from a filename alone.
2. Preserve exact task identity, canonical location, no-symlink checks,
   metadata consistency, active-session absence, and detection of replacement,
   truncation, or content change during observation. Archived evidence must be
   stable: do not import the old active-history append tolerance.
3. Make resource bounds explicit and suitable for long-lived tasks. Bound
   memory by chunks/records rather than total history size. Review both the
   existing byte and line-count limits; give resource exhaustion a truthful
   diagnostic, distinct from invalid identity or malformed evidence. Test
   configured limits without creating huge permanent fixtures.
4. Archival, Git reclamation, and route retirement remain separate. Never
   replay the successful v0.9.7 archive call. Existing command-managed closeout
   revalidates exact preservation and attachment before local branch removal.
5. Keep RC15 retirement operationally separate from the adapter fix. Preserve
   its failure classification and evidence. Use its authenticated source
   runtime for source-owned terminal actions and existing supported semantic
   retirement/closeout commands; no journal edits or identity impersonation.

## Bounded checkpoints

### 1. Authenticate and settle the retirement procedure

Read current package, assignment, Git, and host evidence. Name each remaining
resource, its owning actor, and its supported command. Confirm the accepted
release branch tip is preserved before cleanup. For RC15, establish whether
source-owned abandonment and subsequent archival/retirement are available;
record the exact sequence before executing it. Do not reopen it for coding,
retry its child, relocate source branches, or attempt to turn its failed
canary into PASS. If no lawful sequence exists, pause that cleanup item for a
director decision rather than designing a recovery framework.

### 2. Correct and review the adapter

Implement the bounded-memory reader and focused regressions. Add one concise
ADR and a lesson in the existing lessons/release guidance, tying the missed
gate to realistic long-lived session sizes and actual closeout consumers.
One independent bounded read-only review covers the changed observation and
deletion-authority boundary; do not duplicate the full test suite.

After focused checks, attempt the existing large-session read-only observation
immediately. Reuse that evidence for the candidate's live gate; do not spend
successive checkpoints building instruments without attempting closeout.

### 3. Finish exact cleanup and release

Use a validated candidate through the supported assignment command boundary
to finish the accepted v0.9.7 assignment. Do not edit installed caches or pinned
snapshots. The director performs recipient-owned acceptance/host actions;
the coordinator prepares exact requests and owns implementation and release.
Retire RC15 through the settled procedure, preserving its failure evidence
outside any namespace scheduled for deletion. No force deletion is authorized.

Promote one immutable v0.9.8 release after the changed live boundary passes.
Tag and push through the established repository release process; build from
the exact tag, install the artifact through the personal marketplace, prove
cache equality, and request App reload only when required for live hooks or
loaded skills. No npm publication. Do not overwrite v0.9.7 or publish unused
intermediate releases. The new coordinator's own final delivery and closeout
are part of acceptance; no extra canary hierarchy is required.

## Acceptance evidence

- Focused tests cover a valid history above 32 MiB, bounded-memory streaming,
  metadata/identity mismatch, malformed or conflicting metadata, oversized
  records/resource exhaustion, symlinks, active-session presence, and file
  change/replacement/truncation during observation. Small-file evidence remains
  compatible. Test bounds with compact generated temporary inputs.
- Real observation authenticates the existing 96 MB archived coordinator;
  command-managed retirement finishes with no second archive call, no manual
  journal change, and preserved source history. Its task worktree/local branch
  and active reporting locator are absent; assignment/iteration are retired.
- RC15 is explicitly recorded as failed/abandoned, not successful; its exact
  eligible task and resources are retired with preserved evidence. If blocked,
  report it separately and obtain an explicit disposition before claiming this
  plan complete; it must not silently expand the package release scope.
- Run the full established Node suite once on the final candidate, source and
  schema/import validation, package dry run, plugin/all-skill validation,
  release identity checks, and git diff --check. Reuse unaffected live evidence;
  rerun only checks invalidated by subsequent changes.
- Stable artifact/cache equality, post-reload skill identity, exact final
  report delivery, director acceptance, and the new assignment's closeout.

## Execution authority and escalation

Director owns this plan and final acceptance. After approval, use the direct
skill to dispatch one Terra-xhigh coordinator: this is bounded but demanding
adapter/cleanup work. It may use an explicitly selected cheaper worker for
independent work where worthwhile. One focused reviewer is sufficient.
Use installed stable Flow and immutable run snapshots; select model, effort,
and rationale deliberately. No recurring watchdog unless separately requested.

Escalate before widening deletion authority, bypassing a failed check, changing
evidence schemas or trust semantics materially, modifying a published release,
or introducing unsupported predecessor recovery. Ask the user when an App
restart/trust action is necessary. Approval authorizes delegated implementation
and the bounded release/cleanup actions above, subject to their stated gates.

## Approved amendment: one-time admission clean start

Approved by the user on 2026-09-08 after the coordinator reported the admission
deadlock. This amendment takes precedence over the original requirement to
finish old assignment retirement through normal closeout before implementation.
RC15 is already abandoned, but its missing source archive record prevents a
semantic refresh; its namespace also blocks a fresh run. Unplug includes the
new preparation as well as old assignment/iteration/reporting state.

Authorize the existing v0.9.8 coordinator
`01a07f08-9853-7c11-a31e-56823209de27` to perform one repository-scoped clean
start using the installed stable unplug commands, subject to these gates:

1. Before deletion, preserve exact old Flow evidence, the approved amended
   plan, assignment preparations, accepted release/report evidence, and an
   inventory of affected tasks/routes/Git resources outside `.git/codex-flow`.
   Use the repository's `.git/codex-release-evidence/v0.9.8/` location; verify
   copies and record digests. Preserve the existing large archived App session
   untouched for the direct adapter test. The backup is historical evidence,
   not live authority, and must never be silently restored or migrated.
2. Account for every affected assignment and reporting destination. Preserve
   this director and the existing v0.9.8 coordinator, its worktree and source
   branch; do not archive or replace either. Confirm no active execution run
   or unrelated assignment is being erased. Exact idle old RC15 coordinator
   archival is authorized through the owning App tool if needed; never replay
   an already accepted archival or treat abandonment as successful completion.
3. Derive a fresh unplug plan after those checks. The previously reported
   `unplug-plan-v2-49358c760c6cef9da03ccfc223daccc1e0177abe517bdf77683157006248fd83`
   is diagnostic evidence, not the apply target. Apply only when its inventory
   consists of this repository's reviewed obsolete Flow state and exact eligible
   old release/canary resources. Removal of the obsolete v0.9.8 preparation is
   explicitly authorized after preservation. Keep clean/preserved Git checks,
   deletion-last ordering, and all command safeguards. Unexpected resources,
   active work, protected checkouts, force removal, or another unsupported
   transition require escalation; no ad hoc state edits or deletion commands.
4. Verify the unplug receipt and actual resource postconditions. Record that
   old state was explicitly unplugged with retained failed/abandoned evidence,
   not normally closed, accepted, or semantically migrated. Preserve remote
   refs, published releases, source work, and the user's App project entries.
5. Report completion directly to this director while the old report route is
   absent. The director will prepare fresh assignment authority from this
   amended plan and send it to the SAME coordinator task. Register its new
   reporting route before substantive implementation. Do not reuse old run,
   preparation, or route IDs, spoof the director, create another coordinator,
   or add a permanent recovery mechanism.

The corrected adapter must still authenticate the real large archived session
and pass focused closeout integration tests. Because this approved clean start
removes the original live journals, do not claim it proves normal retirement
of the original v0.9.7 assignment; report explicit unplug separately. Exercise
the remaining normal reporting/closeout boundary with the new assignment and
isolated temporary test fixtures, without restoring old state into authority.
No published package or immutable source runtime may be patched to enable this
one-time procedure. All remaining v0.9.8 scope and release gates are unchanged.
