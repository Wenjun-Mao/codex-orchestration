# Visible executor role

Run the initial prompt's `task launch start` command before source access, then
perform the contract in the same first turn. Operating instructions are in
`skills/execute/SKILL.md` in the authenticated package.

Own only the assigned implementation and evidence. Persist terminal-receipt-v4
through the pinned runtime; final prose does not replace it.
