# Source coverage and remaining gates

Source tests exercise public journeys in disposable Git repositories using injected
native observations. Installed sources `0cae260` and `f627eef` completed the bounded
same-host journeys summarized below. Live evidence qualifies only the exact named
boundaries; fixtures are not native evidence.

| Umbrella issue | Source evidence | Remaining closure boundary |
| --- | --- | --- |
| KI-01 | Failed scope work stays blocking; native failed-check recovery preserved rejected history and completed an independent repair | Native scope-violation and crash recovery remain source-only |
| KI-02 | Existing locks refuse all callers; exact externally quiescent recovery; interrupted ownership facts remain inert | Operational quiescence remains a cooperative precondition |
| KI-03 | Never-enabled revocation rejects late starts; exact ready-ID native early binding returned BINDING_PENDING without writes and resumed with the same command | Provisional-ID reconciliation remains source-only because the host returned ready IDs |
| KI-04 | Source fixtures cover mixed history; native failed-check repair plus product-pilot delivery and a dependent no-change successor preserved retained main history | Broader workloads remain unqualified |
| KI-05 | Exact frozen read/ack/accept separation is source-tested; canary and README-only product pilot completed genuine same-host Stop capture plus `shared-storage` receipt | Cross-host or native-message delivery is not claimed |
| KI-06 | Child-first source gates plus native normal, failed-path, and product-pilot exact task retirement; retained checkouts/branches remained | Broader rollout remains director-owned |
| KI-07 | Generated host identity completed a native three-call start with no lookup; ready-ID early binding resumed safely; conservative accounting fell from 8,974 to 6,106 tokens | The 6,000 target is missed by 106; provisional-ID output remains unobserved |
| KI-08 | Exact 20-file package allowlist, only Node and Relay imports, relocated fixture tests, and one restart-free installed candidate transition | Broader upgrade and rollout remain unqualified |
| KI-09 | Source exclusion plus observational evidence that existing separate-repository Flow reports remained readable with exact digest attestation after Relay install | Stress testing and same-repository dual control are not claimed |

The recovered mixed journey retained coordinator/child commits but required parser
and receipt corrections, restart, and report-only continuation. The corrected
candidate later completed a normal three-call startup and a separate ready-ID early
binding replay without another restart or actor lookup.

Remaining gates are observed provisional-ID reconciliation, Flow-report
stress/same-repository coexistence, and the 106-token provisional-target miss. The
bounded README-only product pilot and dependent no-change successor passed. Release
and broader rollout remain outside this evidence.
Do not mark Flow fixed or describe same-host shared storage as native message
delivery.
