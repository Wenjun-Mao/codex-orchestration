# v0.9.7 exact archived-worktree reclamation

Status: Approved by the user on 2026-09-07.

This narrowly amends the approved assignment-reporting and lean-closeout plan.
Keep the original plan snapshot and historical release authorities unchanged.

## Outcome and decision

Complete accepted iteration cleanup without relying on an unverified promise
that Codex App will eventually remove an archived task's worktree. Flow may
reclaim an exact owned worktree after accepted work and authenticated archival.
Supersede ADR 0036's host-only reclamation rule only at this boundary; retain
the distinction between archival, physical reclamation, and branch cleanup.

## Scope and safeguards

- Apply to eligible iteration executors and disposable coordinators, including
  truthfully registered detached coordinators.
- Before removal, verify exact task/iteration ownership, archived/no-active
  evidence, Git common directory, canonical path, unchanged attachment and
  captured commit, clean state, and preservation in the authenticated
  integration baseline. Protect caller, primary, retained/shared, active,
  ambiguous, dirty, untracked-risk, and unintegrated work.
- Persist the exact cleanup intent through existing closeout authority and
  revalidate at mutation boundaries. Use non-force Git worktree removal;
  a refusal remains pending or blocked, not authority to force deletion.
- Remove only an eligible unchanged local task branch after worktree removal.
  Detached work grants no branch-deletion authority. Never touch remote refs,
  unrelated worktrees, or external side effects.
- Make interruption and already-absent reconciliation safe without repeating
  accepted archival. Retire reporting only when required closeout is complete.

## Bounded delivery and acceptance

The existing release coordinator owns implementation, focused regressions,
ADR/guidance alignment, packaging, and release. The director owns acceptance.
Do not rewrite journals or installed snapshots, perform ad hoc manual deletion,
change global App auto-delete settings, or add a daemon or cleanup registry.

Exercise exact named and detached cleanup, dirty/unpreserved/protected/path or
attachment drift rejection, host-already-removed state, interrupted removal,
and no archive replay. Reuse unaffected evidence; batch known corrections into
one candidate before the next necessary reload.

The cheapest safe direct proof is to resume the already accepted zero-child
canary through the corrected command, verify exact worktree removal and route
retirement, then complete the remaining real child-first canary. Do not repeat
the completed mutation. Stable promotion still requires the approved live and
release gates. Keep cleanup pending truthfully until those postconditions hold.

The temporary cleanup-observer timer is not a correctness dependency. Keep any
remaining watchdog limited to the user's original missed-report insurance,
with the existing restart/completion stop rules.

Escalate any need for force removal, weakened identity/preservation checks,
broader resource deletion, or materially wider cleanup authority.

## Approved delivery correction

The user approved this bounded delivery correction on 2026-09-07:

- Freeze product scope at the approved release and reclamation correction.
  Keep the existing Sol coordinator; do not restart implementation or retune
  selector policy in this release.
- Before the next installation, obtain one independent Terra-xhigh read-only
  review through the existing support lifecycle, with explicit selectors and
  bounded context. Review the candidate's refresh, reporting, acceptance, and
  cleanup path: every required transition needs an available owner/action, and
  tests must exercise the actual runtime, work shape, identity, and Git state.
  Return actionable findings with evidence, not a general audit or new harness.
- The coordinator owns review dispatch, fixes, and integration. Consolidate
  connected findings before another install/restart, then attempt the existing
  canary's cleanup and real child-first lifecycle. Do not turn the review into
  successive instrument-only checkpoints. Reuse unaffected evidence and rerun
  checks invalidated by changes; retain all required stable release gates.
- Preserve the reusable lessons briefly in existing release guidance: test real
  lifecycle shapes, identify the owner of every transition, and reassess
  connected failures before another installation. No new framework, benchmark,
  retrospective project, or director monitoring loop is authorized.

For subsequent assignments, choose the delivery owner by the hardest expected
judgment, not child count; the coordinator still owns the detailed staffing
decision. This does not change the current coordinator or package defaults.

## Approved one-time RC15 canary recovery

The user explicitly approved this exception on 2026-09-07. The child-bearing
canary was registered detached, but its immutable runtime requires integration
from that same checkout on an attached target branch. This is recovery of a
failed canary, not a new package contract or a successful first-turn gate.

The release coordinator may temporarily relocate the existing local branch
`codex/v0.9.7-skill-pruning` from the primary checkout
`/Users/wjmao/projects/utility_projects/codex-orchestration` to the existing
canary coordinator checkout
`/Users/wjmao/.codex/worktrees/6233/codex-orchestration`. Integrate only the
preserved child commit `6a8e5ed3e3b0ae6ccfc9df6ee1573bf88c3c45c4` through the
existing RC15 preparation, verification, reconciliation, and disposition
commands. Use the immutable run snapshot and each actor's actual identity.

Before mutation, record exact checkout/ref tips and the restoration sequence,
verify both checkouts clean and exclusively available, and stop overlapping
release Git operations. Restore the release branch to the primary checkout and
the canary to its registered detached attachment, preserving the integrated
result, before child-first archival and normal closeout. Revalidate at each
boundary; interruption requires inspection and continuation from actual state,
not blind replay or reset. Keep director acceptance separate.

No identity spoofing, journal/snapshot rewrite, direct-library CLI bypass,
force operation, new child, remote-ref change, or unrelated resource mutation
is authorized by this exception. Stop on unexpected drift or an unavailable
restoration/closeout step. RC16 installation remains held until this recovery
and the corrected canary setup are executable. A fresh RC16 canary must still
prove first-turn activation and the full child-first lifecycle. Reuse unaffected
verification; this exception does not authorize a recovery framework.

## Approved owning-host archival correction

The user approved this scope change on 2026-09-07. Replace the all-in-one CLI
archive requirement with deterministic Flow preparation, an exact Codex App
archive tool call by the owning role, and Flow reconciliation/reclamation.
Flow still derives membership, eligibility, ordering, and pending actions;
neither the user nor the model reconstructs task lists or cleanup authority.
This supersedes only the requirement that the CLI itself execute the host call.

For the existing failed RC15 canary, the release coordinator is authorized to
arrange exactly one recovery archive call through the owning Desktop host for
child task `01a07df5-bf4e-7cb3-861e-78882c1ccae2`. Before calling, confirm exact
identity, idle state, preserved accepted integration, and absence of already
completed archival. If archival is independently confirmed, reconcile without
another call. Preserve the original ambiguous attempt
`iteration-archive-attempt-v1-a517660b81feb6779ca113139ec61f9b672e4dac9b4b05da892bed0035469188`
and record the explicit recovery authority and actual host result. Resume the
existing RC15 closeout through supported commands using fresh archive evidence;
do not rewrite journals or snapshots or manually delete worktrees/branches.
Another ambiguous recovery result stops; this is not recurring retry authority.

Fold the bounded adapter/command contract correction, focused regressions,
guidance, and concise ADR into the current unreleased candidate. Keep reporting
transport changes out of scope unless causally required. No additional
app-server archive fallback, daemon, private IPC mechanism, or new lifecycle
registry is authorized. Test the actual Desktop-owned idle-task shape, exact
prepared action/result matching, interruption, ambiguity, already-archived
reconciliation, and child-first cleanup without replay. Preserve useful RPC
error details with bounded, non-sensitive diagnostics.

Recovering RC15 does not pass its failed first-turn gate. Before the next live
candidate, establish the fresh canary's owned integration branch before run
activation and assignment registration. Reuse unaffected evidence, retain the
remaining live and stable release gates, and pause for any further material
authority expansion or necessary App restart.
