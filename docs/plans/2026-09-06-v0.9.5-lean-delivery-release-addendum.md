# v0.9.5: lean delivery and shared release validation

Status: **Approved bounded extension on 2026-09-06**, based on the user's request to add lightweight staffing while avoiding overlapping testing and release work. The director selects one final v0.9.5 shipping checkpoint; the original reporting outcome and safety gates remain unchanged.

This addendum extends [the approved planning and reporting plan](2026-09-05-v0.9.3-planning-and-queued-reporting.md), whose original SHA-256 is `e4d39b952c0f3b6c909a4abede4a9a963f7ccf16b8d1428c5769818583931622`. It does not rewrite that approval or treat published v0.9.3 as having passed the still-missing reporting acceptance gate.

## Outcome and authority

One bounded delivery task can complete a simple assignment and return its full report without requiring a Sol coordinator plus a separate executor. The director remains available; delivery ownership, exact runtime authority, verification, integration, reporting, and cleanup remain explicit.

The causal question is whether optional delegation and workload-based model choice can remove an unnecessary task while preserving delivery accountability. The cheapest safe direct attempt is one authorized, bounded Terra-high delivery task with no children, completing useful work and reporting through the corrected same-host route after a same-sender lifecycle transition.

At the director's check, stable v0.9.3 is published, the reporting correction is in progress, and no local v0.9.4/v0.9.5 tag exists. Keep the reporting correction and routing change separately reviewable, but target one final public v0.9.5 release instead of shipping and installing an intermediate v0.9.4 solely for sequencing. If v0.9.4 has already been published concurrently, preserve it unchanged and disclose the scheduling conflict. Never rewrite historical tags.

## Two bounded code checkpoints

1. Finish the already-authorized reporting correction: exact authenticated sender-locator retirement; exception/crash-safe recipient, route, and locator registration; preserved pending/ambiguous evidence; restart/re-registration regressions; and correction of contradictory packaged instructions. No manual orphan deletion or permissive authority bypass.
2. Align selector policy and role guidance: coordinator is a responsibility, not a mandatory Sol model or a promise to create children. Permit explicit Terra-high coordination for settled, bounded delivery with established verification. Retain Sol-high as the recommendation for substantial uncertainty, systemic decisions, or complex multi-executor coordination. Existing deliberate overrides remain explicit and justified. Do not assert these recommendations are empirically optimal.

A coordinator may perform its entire bounded assignment directly. It delegates only when an additional task has a concrete benefit; no mandatory executor count or extra delegation ceremony. The lightweight task retains its existing coordinator delivery authority, not a relabeled executor contract. Do not introduce a second direct-executor lifecycle, new role schema, automatic model escalation, or availability probes. Narrow mechanical work may still use the existing Luna-xhigh recommendation. Publishing and destructive cleanup still require their established authorization and checks, regardless of model.

The current Sol-high coordinator continues unchanged. No replacement coordinator or new implementation lane is mandatory. Do not silently change any active task's selectors, runtime, or authority.

## Share verification without weakening evidence

- Run focused regressions for each code checkpoint while it is developed. Preserve the checks and causal evidence from the reporting repair; do not postpone its first safe direct attempt merely to accumulate more instrumentation.
- Run the complete Node, schema/source, package, plugin, skill, dependency-direction, and release-identity gates against the combined final candidate. Reuse unchanged supporting evidence only within its exact source, artifact, runtime, and host applicability. Changed behavior invalidates affected evidence.
- Use one combined live scenario where feasible: the corrected same-sender refresh/retirement/re-registration path leads to an explicit Terra-high coordinator with a bounded approved assignment, zero child tasks, real verification/closure responsibilities, and a full final queued to this director while it is available for discussion. Keep the sender task identity constant across the re-registration segment; model choice changes only through an explicitly declared new assignment.
- Verify actual incoming report text and identity, not just queue acceptance or manual retrieval. Retain the original busy-recipient, literal wait_threads, ambiguity, late-report, and lifecycle safety gates; the combined scenario does not replace distinct conditions it does not exercise.
- Add lean policy/behavior checks proving a simple assignment can avoid delegation, a complex assignment still receives a justified staffing recommendation, and verification/release ownership does not return to the director. Do not add a cost benchmark or new test framework.
- Every failed App-facing segment still requires root-cause classification, a regression, and a passing repeat. One artifact's canary does not certify another version automatically.

Aim for one final packaging/install cycle and share necessary live-test sessions. Do not promise a fixed restart count: changed hooks, unloaded skills, or final stable authentication may still require genuine user trust/restart actions. Never bypass them to save time.

## Release and exit

Only after the combined RC and original essential live gates pass, promote/tag/push immutable v0.9.5, build from its exact source, install from its artifact, prove cache equality, and authenticate the final stable installation in the same long-lived director after required reload. Finish exact checkpoint-owned cleanup and trial retirement only after replacement reporting is live-proven. Preserve concise release evidence and the previously reported failures.

Do not restore the watchdog the user removed. Do not contact or modify pilot projects, read/write AGENTS.md, migrate arbitrary predecessor journals, retry ambiguous delivery, or expand into a daemon, router-training project, or second orchestration engine. Routine implementation decisions stay with the coordinator; material changes to outcome, safety gates, or external authority return to the director/user.

Reporting recipient remains director task `01a04b13-a1ef-7731-a63b-819b7f8cb150` on host `local`. Return one complete final through the live-verified quiet path. Retain explicit manual escalation for a genuine blocker or necessary user action until that path is proven.
