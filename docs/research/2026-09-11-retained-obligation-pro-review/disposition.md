# Review disposition

Status: proposed plan amendments, not implementation approval.

Both reviewers inspected plugin commit
`634e86c0863359e94a63d8f15dc97905a079d57a`; neither ran Flow tests or reproduced
the pilots. Originals and Reviewer A's supplied Git probes/results are preserved
unchanged alongside this note. Their agreement is not acceptance evidence.

## Use

- Derivation-first: prefer a shared interpretation of current outstanding
  obligations consumed by existing explicit acceptance and admission paths.
  Do not default to a new command, receipt, or mutable settlement flag. Require
  a demonstrated missing durable fact before adding one.
- Preserve three independent judgments: product acceptance, execution scope
  compliance, and safe resource retirement. Failed/noncompliant execution need
  not become compliant to permit safe retirement.
- Include every consumer: acceptance, CLI activation preflight, native retained
  fence conflicts/reporting, historical and sibling classification, and existing
  ownership/closeout. Same-package overlapping successor work is the decisive
  admission test, not disjoint read-only verification.
- Resolve the admitted obligation/producer graph rather than filtering literal
  claim/launch state names. Interpret repository-local resource reservations via
  their existing contract (ADR 0017), not an invented external-resource service.
- Add a separately approved coordinator committed-scope companion: task and run
  coverage, completion and audit consumers, exact child-integration attribution,
  and Git identity recheck after supplied verification commands.
- Scope compliance should cover reachable committed transitions, including
  reverted violations, not merely net final-tree changes. Do not claim coverage
  of transient uncommitted writes or unavailable/rewritten history.
- Preserve existing patch-equivalent preservation, archived ownership checks,
  open-assignment historical cases and expected replay behavior. Proof must
  survive authorized closeout updates and legitimate checkout reclamation.

## Test before treating as established

- Whether existing durable records suffice without a supplemental receipt,
  especially after coordinator reclamation. The no-new-state approach is a
  preferred design to prove, not yet an implemented solution.
- Public CLI reproduction of scope violation; old-producer completed evidence
  must not bypass candidate audit enforcement. Include an inherited-file control,
  between-operation changes, child overwrite and merge-attribution cases.
- Connected accepted/abandoned settlement, real host closeout, interrupted replay,
  and useful overlapping successor under one package plus frozen v0.9.12 input.
- Exact pilot eligibility: physical cleanup counts are not complete logical
  obligation proof. No review authorizes pilot state mutation.

## Correct / qualify

Reviewer A correctly identifies conditional no-replacement clean-start refresh.
Source inspection confirms the accepted-assignment restriction in prepare is
conditional on replacement work. Reviewer B's general statement that refresh
requires an open assignment is too broad. This does not establish either pilot's
eligibility; differing package authority and source-namespace retirement make it
different from ordinary same-authority settlement.

The reviewers' advice to keep Plotloom paused concerns its Flow lifecycle. The
user subsequently authorized source-only product continuation, with original
Flow evidence preserved; this review does not revoke that separate exception.

## Park / scope decision

Ordinary same-assignment replacement after mid-run replanning is a separate
binding contract. Do not quietly add it to this accepted-delivery closeout fix.
Recommend explicitly excluding it for this checkpoint and narrowing claims:
this correction would resolve settled abandoned history and coordinator cleanup,
not every future mid-assignment planning omission. A user-approved expansion is
needed if ordinary in-assignment replacement is required now.

Do not merge the premature-progress-final observation or wrong-runtime-root
recovery into this checkpoint. Discard suggestions of blanket audit trust,
blind retained-to-released updates, or new global settlement machinery.

## Local verification

Inspected relevant source consumers and ADR 0017. Ran the supplied 94-line
Git-only probe script after reading it in full. It creates/removes its own
temporary repository and does not invoke Flow or touch pilots. All three
assertions passed on Apple Git 2.54.0:

1. Endpoint diff misses a committed addition later removed.
2. Rename-detecting name-only output omits the deleted source location.
3. Subtracting child filenames hides a later coordinator edit to the same file.

The attached JSON is Reviewer A's original run, not this local rerun. These
probes establish comparison pitfalls only; they are not Flow regression or
release evidence. No full suite or live lifecycle was run for this review.
