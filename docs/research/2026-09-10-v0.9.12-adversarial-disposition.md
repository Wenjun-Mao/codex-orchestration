# Adversarial review disposition — v0.9.12

Status: Director review; proposed focused verification, not expanded implementation
or release acceptance. September 10, 2026.

## Evidence boundary

Preserved unchanged:

- `2026-09-10-v0.9.12-adversarial-existing-pro.txt`
- `2026-09-10-v0.9.12-adversarial-new-pro.md`

Both consultants inspected exact baseline `ee24c2382a20acb0d50ab4a7dd8a7981653ec163`.
Neither executed tests or inspected private journals or ongoing implementation.
Director inspection compared their source chains with the coordinator's uncommitted
worktree based on `88a52b4` on September 10. That snapshot is not a finished or
accepted candidate. No reproductions were run during this review.

## Disposition

| Finding | Decision | Local check and bounded follow-up |
|---|---|---|
| Audited result versus later archive tip | Use + Test | Draft passes the exact audit's `expected_head_revision` into iteration validation and checks audited-tip ancestry into archive tip, then archive-tip preservation in primary. It already blocks the reported loss shape. Confirm post-close/pre-capture rewind rejection and the safe variant where primary preserves the audited result independently; requiring audited-tip ancestry into archive tip may be unnecessarily restrictive. |
| Standalone versus lost registration | Use + Test | Draft requires one exact retired assignment for reclaimed modern history and has a standalone-refusal test. Preserve this explicit evidence limit. The assignment-only interruption/lost-record variant is stronger than deleting a fully retired record with surviving backlinks. No new standalone certificate is proposed. |
| Historical versus effective resource identity | Use + Test | Draft uses effective resource correction but compares its path/branch with runtime-context path/branch supplied by the caller. Exact execution authentication and effective cleanup ownership need separate checks; exercise lawful correction and refreshed execution bindings before claiming coverage. |
| Cancellation after refresh apply, before target activation | Test — highest priority | Current lookup excludes cancelled assignments; binding returns `no-open-assignment`, and target preparation/admission occurs before that binding. Source ordering is confirmed, composed reachability remains unexecuted. Extend existing assignment-lived refresh test with this ordering and an open-assignment positive control. If reproduced, association lookup must include terminal states and permission must be serialized with cancellation before preparation, not merely checked earlier without the owner lock. |
| Unrelated retained namespace changes before refresh-ID admission | Test | Ordinary admission invokes predecessor classification; refresh consumption checks the named source but not the same repository-wide history. Combine one semantic handoff and one unrelated retained namespace, then invalidate the latter on an isolated negative copy. Preserve legitimate source-specific continuation and distinguish first admission from bookkeeping replay. |
| Target Git changes inside ordinary activation | Test | CLI snapshots Git before asynchronous bundle/lifecycle work; locked fresh admission does not resnapshot before preparation. A deterministic in-invocation drift test is distinct from changing evidence before starting activation. If reproduced, compare current target identity/cleanliness with the derived runtime immediately before preparation. This does not claim protection against arbitrary external writes at every subsequent instant. |

## Recommendation

Keep the shared historical-classification architecture and existing main positive
journey. Do not interpret six review points as six new implementation workstreams.
The first three refine planned evidence checks; the latter three expose specific
admission-ordering cases not established by the ordinary multi-namespace journey.

Reproduce cancellation-before-consumption first. Incorporate focused variants of
existing fixtures and owner-level corrections only where warranted. Do not add
another broad matrix, registry, browser bridge, live task, or RC merely because
an external reviewer proposed a sequence. A material lock/authority redesign
still requires a bounded decision before implementation.

These reviews support the direction, not its completion. Existing draft coverage
and source inspection are not substitutes for completed tests or live acceptance.
