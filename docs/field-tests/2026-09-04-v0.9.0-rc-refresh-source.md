# v0.9.0-rc refresh-source canary

Date: 2026-09-04  
Source authority: v0.8.3  
Target: v0.9.0-rc.2

## Field-test result

One completed v0.8.3 executor result was preserved in the source baseline
before the coordinator refresh. The result remains an ordinary durable
executor outcome: it is integrated serially, verified against the combined
baseline, and retained as the source-side evidence for the refresh.

The refresh boundary therefore carries semantic result evidence rather than
reading or migrating v0.8.3 journals. Any unintegrated executor-local work is
separate and may only be reissued through a fresh task, launch, branch,
worktree, selector, and rationale identity.

## Authority and scope

- The v0.8.3 result is the completed source-side input to the canary.
- The v0.8.3 source coordinator remains responsible for integration, combined
  verification, disposition, and archival before refresh.
- The v0.9 coordinator consumes only the semantic refresh handoff and retains
  the already-integrated baseline result.
- This note records the bounded field-test outcome; it does not promote an
  unverified result or authorize a second executor attempt.

## Exit condition

The canary passes this slice when the completed source result is still
available in the baseline after refresh, while the refreshed coordinator
accepts only the authenticated semantic refresh export and does not parse or
migrate predecessor journals.
