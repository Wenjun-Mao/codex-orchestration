# UK Dev v0.3.1 lifecycle field test

- Date: 2026-08-23
- Package candidate: `2b931582804a21588947bd2b1b7f92e94dbcceee`
- Repository branch: `codex/codex-flow-v0.3.1-uk-dev-pilot`
- Repository revision: `755e22f0202a06d8ae04b01551ad1f71c26691d1`
- Result: PASS for install, local baseline guard, visible task creation, callback
  lifecycle, doctor, and audit-only cleanup

## Installation

The pilot created its dedicated branch before planning. The external-AGENTS
plan `de6762f60ecde81df1d786cfdcd97af89bc1fff7d04ce0c9fe72097c0718c82f`
was read-only and applied on the same unchanged branch. It installed 29 pinned
runtime/config files, proposed no `AGENTS.md` operation, and preserved the root
instructions at 49 lines with SHA-256
`02a21283d993fc99c7d0ad33b1462ede903c25d8d62b76da68b579708d1f9c84`.
The repository's existing process validator, `init --check`, and v0.3.1 doctor
passed.

## Baseline guard

A structurally valid local task packet deliberately supplied the seven-character
prefix `755e22f` instead of the exact full revision. `task operation prepare`
failed with a baseline-mismatch error. Before/after operation-journal snapshots
had the same SHA-256
`2f1b255728cfcb0764da05322788bf4fe93e21074e38429e700d52429b17b4f7`,
and no host creation call was made for that packet.

The positive packet derived the absolute worktree root, full revision, and
cleanliness from Git. Operation
`task-operation-v1-5ac2786d49413c474313d58b7d93372635422c14f9774fc743e8c994287c6c09`
persisted those fields, started one attempt, and was reconciled to one exact,
user-visible `task-thread` titled `Codex Flow v0.3.1 UK Dev Probe`. The request
recorded `gpt-5.6-terra` and `xhigh`; host list/read surfaces did not provide an
independent model/reasoning field.

## Callback and cleanup

Callback
`terminal-v2-32a57f07ef40587835caa1b6404cddd5cbf0e0996957d08ecedc3e589a797a4b`
was persisted, enqueued, observed, and consumed exactly once. Final status had
two historical consumed callbacks, zero pending, zero expired, and zero
superseded. Doctor reported no errors, warnings, runtime drift, or ambiguous
task operations. Cleanup audit was read-only, recommended no action, held no
leases, and reported 8.63 KiB of repository-scoped state.

## Host caveat

One bounded thread-list call took roughly eleven minutes before the exact host
object could be reconciled. After creation, Slingshot list/read controls became
unavailable and two idempotent archive attempts timed out without a usable
result. No create retry occurred, and the late positive observation was
reconciled to the original operation. Archive state is therefore not claimed
by this field-test record. This is a host-adapter liveness/observability
limitation, not a callback or baseline-authentication failure.

The pilot branch is pushed, clean, and intentionally unmerged. No active UK Dev
executor was migrated.
