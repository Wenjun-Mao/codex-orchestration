# Relay 0.3.0 RC1 hook-owned candidate

Candidate only; not installed globally or released stable.

Source: `6deb876`, followed by `d06815f` deriving CLI help version from the packaged
manifest. The package gate caught the old literal version; this correction removes
the duplicate version source instead of weakening the check.

- Final runtime suite: 40/40 passed in 39.534 seconds.
- Relocated package suite after the CLI correction: 25/25 passed in 9.348 seconds.
- Plugin and both changed skill validations passed; whitespace check passed.
- Attended independent read-only review found two P2s: malformed JSON field types
  could escape callback error handling, and absent process close could outlive the
  cleanup deadline. Both were fixed and covered; reviewer confirmed no remaining
  P1/P2 findings in the reviewed transport/reporting diff.
- Artifact: `.git/codex-release-evidence/relay/0.3.0-rc.1/relay-0.3.0-rc.1.tgz`.
- SHA-256: `49e95d355beb6fb46bc7f248381e3861b165a90380f0d3e9f3cff113b878f337`.
- Package: 27 files, 42,153 compressed bytes; no Flow runtime dependency.

## Native gate setup

The existing `relay-native-canary-20260912` project is clean at `343db9e`, with
project-local hook/config files committed. The hook points to the artifact-derived
package above; project config disables installed `relay@personal` only in this
canary to avoid two hooks capturing the same event. User/system hook trust remains
untouched. A fresh manager task is requested for setup only, with no workers or
Relay assignments admitted before trust is established.

This isolation changes neither the personal marketplace nor the global installed
Relay 0.2.1. Plotloom was not contacted or changed. New/changed hook trust must be
handled by the user if the App requests it; do not bypass that boundary.

Still required: genuine candidate Stop capture → exact queue → idle manager wakeup,
question/answer/resume, sequential child and parent review/idle/archive, and useful
successor delivery. The earlier direct queue probe is transport evidence only.
This project-local test does not by itself qualify global plugin discovery/update.

## Trust boundary

Fresh manager `01a09741-d93e-7a03-b988-ed3a212ba14e` completed setup and is idle.
Its statement that no hook-review requirement surfaced is not trust evidence.
Read-only native `hooks/list` for the exact canary cwd reports the project Stop
hook enabled with `trustStatus: untrusted`, hash
`sha256:3fa26a9d2e2912f2322d042b31bc7e098085e606a4bc92dcef8e40f48acec7bf`.
The installed Relay hook is absent from that project listing, confirming local
isolation. No workers or Relay assignments have been admitted for this gate.
Await user hook trust, then recheck before dispatch; no trust bypass or global
plugin installation occurred.

User subsequently confirmed trust. A fresh native `hooks/list` returned
`trustStatus: trusted` for the same exact hash. Existing manager task
`01a09741-d93e-7a03-b988-ed3a212ba14e` was instructed to run the bounded connected
journey, preserving actual hook events and keeping all product repositories and
the global installed plugin unchanged. Live acceptance is in progress, not passed.

The first prepare was refused before task creation: old canary control retained
checkpoint `7a61296`, while test-hook setup advanced main to `343db9e`. The outer
director verified a clean retained main, preserved ancestry, no source permission,
nine archived historical tasks and one revoked unbound record. Under the user's
standing cheap-unplug policy, only the real nonsymlink canary `.git/relay` directory
was deleted, without a new backup. No task replay or source/ref/trust changes.
The manager resumed fresh admission. This is a disclosed pre-admission setup
intervention, not an uninterrupted first attempt or a plugin runtime correction.
