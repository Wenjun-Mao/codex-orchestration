# Codex Flow v0.8.1 coordinator-identity boundary acceptance

## Scope

This field test exercised the v0.8.1 release candidate from the same
long-lived coordinator task that implemented it. It tested the two new
admission boundaries directly: current coordinator identity before run state
creation, and selector observations only after a ready visible-task identity.

No pilot repository or user was contacted. The canary changed only this
repository and its exact Codex Flow task, worktree, branch, and run state.

## Candidate authority

| Evidence | Exact authority |
| --- | --- |
| RC source commit | `7ba939a008ff9b1fdb140c478ebe0dda893fe784` |
| RC version | `0.8.1` |
| RC artifact SHA-256 | `4d37c3abb5795002c419fe39beb718da980987a1e0c8155bba896c6e477e7a7c` |
| Installed cache | `/Users/wjmao/.codex/plugins/cache/personal/codex-orchestration/0.8.1` |
| Run-bound runtime bundle | `65d035d78ca011609c69a1270506847831ac44e1876e586a4a6b51f7d112c2ba` |
| Coordinator task | `01a04b13-a1ef-7731-a63b-819b7f8cb150` |

The same coordinator first attempted activation with a deliberately different
task identity. The CLI rejected it before creating the v0.8.1 RC namespace.
Activation with the host's actual `CODEX_THREAD_ID` then succeeded and
disclosed a matching `coordinator_identity` record.

## Visible-task canary

The canary created Luna-medium task
`01a06979-3be1-7d21-8a10-2b025169ecc8`. The App initially returned only
provisional identity
`client-new-thread:144d7327-6eb3-48f8-bf47-62ce6146e600`.
Provisional reconciliation accepted an explicit null observation. Private
resolution then recovered the nonce-authenticated ready task and its observed
Luna-medium selectors; only that ready reconciliation persisted selector
observations. The exact worktree was bound before objective release.

The task found one stale RC namespace expectation in
`test/cli-v07.test.mjs`, committed the one-line correction at
`af9934ff04e9bec541312fc47e19d929abcd185e`, and persisted PASS callback
`terminal-v3-cdb1c06b244fdbd019b3ed3dedc9b312c9ab670c9702e47e688b7a6ce10bcf98`.
The coordinator integrated it as patch-equivalent at
`5ca6d5e6b9e82eb385b6355cf08573cbf20261cb`.

The first combined-verification record retained a legitimate timeout FAIL:
the full suite exceeded the default 120-second per-check budget. A second,
content-addressed verification with an explicit 300-second budget passed all
209 Node tests, source validation, and `git diff --check`. This was a request
budget issue, not a source-test failure.

The executor briefly edited before `release accept`, contrary to the execute
skill's accept-before-work instruction. It restored the exact pristine
baseline, accepted the release, and reapplied the same isolated commit. This
did not expose a missing runtime contract; the existing release gate failed
closed as designed. The event remains recorded here as operational evidence.

## Cleanup and result

The terminal disposition, integration, and PASS verification were finalized.
The App archive was independently observed, its host-managed worktree was
reclaimed asynchronously, and the exact local canary branch was removed only
after a cleanup plan proved the expected tip, no attachments, and no remote
ref. Run audit
`run-closure-audit-v1-14acdf3182b1afe99f3fd99bbd71fab2f491caad5e6332e8e4837c8d371c4627`
reported zero blockers and zero cleanup candidates; the RC run then closed.

PASS. The live App rejects wrong-coordinator activation before state mutation,
preserves provisional selector uncertainty, admits observations only with a
real ready task, and completes the full integration/archive/cleanup lifecycle.
