# UK Dev v0.4.2 urgent UX pilot

## Scope

This field test exercised canonical package revision `67d655d` in a disposable
UK Dev branch from base `0054c97`. One visible Terra/xhigh host-worktree task
tested the breaking urgent CLI, released-path guidance, ordinary completion,
integration, and cleanup. It did not manufacture a host replay.

## Installation and launch

The exact external-AGENTS plan applied without changing the repository's
49-line root instruction file. The removed urgent consume flag failed during
argument parsing, and the complete journal hash was byte-identical before and
after. Desktop first returned a setup-only task ID; bounded list/read recovered
the exact visible task and host-created worktree. Git binding authenticated the
pristine detached baseline and claimed the packet-declared executor branch
before release.

The released packet clearly distinguished the saved `repository_path` from the
authoritative bound `worktree_path` and stated that no upstream is expected
before first push. The executor authenticated both facts without treating them
as blockers.

## Delivery result

One persisted urgent signal prepared one `host_prompt`, made exactly one direct
host call, and reconciled `sent`. Recipient observation returned `process` and
the exact `consume_arguments`, including the sender executor ID. Consumption
returned `consumed`; repeat consumption returned `already-consumed`. No retry,
host replay, or additional sender attempt occurred.

The ordinary callback was observed once, integrated after review and reproof,
and consumed once. Doctor and canonical checks passed. The exact cleanup plan
removed the executor worktree, local branch, and fetched remote branch; the
final backlog was empty and the task was archived.

## Interpretation

- v0.4.2 is suitable as the default for newly launched UK Dev executors.
- The breaking urgent UX and host-worktree guidance behaved as designed.
- Model and reasoning remain host-accepted rather than independently observed
  when list/read omits those fields.
- Natural host-replay suppression remains unclaimed field behavior and covered
  by deterministic regression only.
