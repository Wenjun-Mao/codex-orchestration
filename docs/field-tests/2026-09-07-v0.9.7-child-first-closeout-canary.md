# v0.9.7 RC15 child-first closeout canary

Date: 2026-09-07

## Observations

The exact existing launch `rc15-child-first-closeout-canary` was re-run once
after its first activation attempt reported a coordinator route sender-host
conflict. The retry used the original launch ID and nonce and returned an
active launch with `execution_permitted: true` and `dispatch_permitted: false`.
No replacement task, scope extension, or second executor launch was created.

The authenticated launch bound the executor to thread
`01a07df5-bf4e-7cb3-861e-78882c1ccae2`, branch
`codex/rc15-child-first-closeout-canary-executor`, and baseline revision
`1c1dae0b83b35b5d565423cf868f9080e758f6c7`. Its generated contract contains
one dependency-free task, `rc15-child-first-closeout-evidence-note`, whose
only write path is this file. Native creation evidence remained provisional;
no ready native thread ID was asserted.

## Gates pending at this commit

This commit is the child-owned evidence result only. The coordinator must
integrate the result through the child-first closeout path, then perform and
verify cleanup from the exact observed child identity. Report-route delivery,
native archival, coordinator acceptance, and run closeout are separate gates
and are not claimed by this note. No delivery or acceptance evidence should
be inferred from the active launch record or this commit.
