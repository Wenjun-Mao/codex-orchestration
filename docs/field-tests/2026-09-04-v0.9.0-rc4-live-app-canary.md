# v0.9.0-rc.4 live Codex App canary

Date: 2026-09-04
App: Codex Desktop `26.901.31953` (`7868`), bundled CLI `0.153.1`
Candidate: `v0.9.0-rc.4` at `3060c93fcdb41cb87117ecc92ffb4e19e3cc2528`

## Result

The same long-lived Sol-high coordinator completed the required v0.8.3 to
v0.9 release canary. One finished v0.8.3 result remained embodied in the
repository baseline. One unintegrated executor was archived and discarded,
then only that semantic assignment was reissued with a fresh task, launch,
branch, worktree, selector, and rationale.

The v0.9 executor received its full contract as the initial prompt. Its first
mutation-capable operation was the exact `task launch start` command, which
authenticated the run-bound runtime, established the real task identity, and
attached the reserved branch. Useful work continued in that same first turn.
There was no bootstrap-only final, waiting-for-release state, coordinator-side
branch-binding wait, normal-path history scan, or second release message.

The candidate then completed callback, serial integration, combined
verification, disposition, archive observation, worktree reclamation, branch
cleanup, terminal audit, run close, and exact unplug. The final unplug receipt
reported `residue: false`.

## App adapter evidence

The App creation call returned only provisional evidence:
`client-new-thread:7e06ef2d-c1f8-41fa-b4af-e21f3ea0a65d`. The executor start
claim independently established ready task
`01a06dc9-860b-7471-be2e-56fc74347fb6` in worktree
`/Users/wjmao/.codex/worktrees/76d1/codex-orchestration`. The known provisional
result and exact start claim reconciled without a private history scan or
identity inference from title or project.

- Native creation returned in 0.702 seconds.
- Flow activation completed about 14.36 seconds after the native call.
- The executor turn completed in about 229 seconds, including task work,
  verification, commit, and construction of its terminal receipt.
- Exactly one initial prompt was sent; no release or objective follow-up was
  sent.

A small Codex App update and restart occurred immediately before the closing
segment. The loaded RC4 skill catalog, provisional creation result, executor
identity claim, worktree activation, private archive observation, and cleanup
all continued to work on the updated host.

## Failed RC segment and correction

RC3 exposed a governance-core race, not an App-adapter identity failure. The
executor truthfully completed before the coordinator reconciled the App result,
so its immutable receipt recorded accepted and observed selector evidence as
null. Later matching App reconciliation enriched launch authority, but exact
byte-for-byte revalidation rejected the earlier null evidence and blocked
disposition.

RC4 made selector evidence monotonic: configured and requested selectors remain
exact; null terminal accepted or observed evidence remains valid after later
matching enrichment; non-null terminal evidence still requires exact non-null
authority. The full callback-before-reconciliation segment was repeated under
RC4 and completed successfully. Contradictory or invented evidence remains
rejected.

One coordinator-authored verification request initially used the nonexistent
macOS path `/usr/bin/test`, producing a retained FAIL record with exit 127. The
lawful content-addressed replacement used `/bin/test` and passed. This was a
canary request error rather than an emitted package recovery command or App
adapter defect; the authoritative PASS record alone was bound to integration.

The executor also made two temporary receipt-construction mistakes before
durable callback mutation. Schema validation rejected both, the request was
corrected to exact launch authority, and one callback was persisted. This
confirmed fail-closed input validation without replaying a persisted delivery.

## Stable release gate

The App-facing canary is accepted for stable v0.9 promotion. Stable source must
still pass the complete automated suite, source/schema parity, package dry run,
plugin and skill validation, annotated-tag identity, and exact artifact/cache
equality before `v0.9.0` becomes package authority.
