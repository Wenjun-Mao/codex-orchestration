# UK Dev v0.5.1 exact-state replay

## Scope

This held-out pilot exercised canonical package revision `b3d933a` in a fresh,
disposable UK Dev adoption worktree. It specifically retested the v0.5.1 host
placement and rejected-before-release checkpoint after mutable state moved from
the shared v0.5 namespace to the exact `.git/codex-flow/v0.5.1/` namespace.

The earlier rejected pilot was removed only through its proven disposable
worktree and unpushed branch ownership. Retained `.git/codex-flow/v0.5` evidence
was fingerprinted before and after the replay and remained byte-identical.

## Installation

The pilot retired the pinned v0.4 runtime on its adoption branch, generated a
read-only external-AGENTS installation plan, and applied that exact unchanged
plan. The repository's 49-line root AGENTS file remained byte-identical.
Canonical `init --check`, pinned `doctor`, the repository process validator,
and diff checks passed before task creation and after integration.

Pinned v0.5.1 reported its mutable state root as exactly
`.git/codex-flow/v0.5.1`. It neither parsed nor changed retained v0.5 state.

## Host-worktree lifecycle

One visible Terra/xhigh task was created under the saved UK Dev Codex App
project with the no-action bootstrap. The host worktree was independently
observed as pristine, detached, distinct from the source checkout, in the same
Git common directory, and at the exact baseline. Codex Flow claimed the declared
executor branch before releasing a one-file evidence objective.

The host call accepted the exact project, model, and reasoning request. Current
list/read surfaces independently exposed the task title and execution path but
omitted project, model, and reasoning fields. The operation therefore retained
honest `host-accepted` partial evidence rather than claiming independent
observation. No contradictory non-null placement appeared.

## Integration and cleanup

The executor committed and pushed only its owned field-test note. Its ordinary
journal callback remained persisted during review, then moved through one
observe, serial integration and combined reproof, and one consume. Repeated
observe and consume calls returned `already-consumed`.

Deterministic cleanup removed the exact executor worktree plus its local and
remote branches. The visible task was archived. Final audit reported no pending
callbacks, leases, eligible Git cleanup, or unbound host worktrees. The pilot
branch stayed clean, pushed, and intentionally unmerged to UK Dev main.

## Result

PASS. v0.5.1 is suitable for newly launched UK Dev executors. Independent host
observation of project, model, and reasoning remains unavailable and unclaimed;
their accepted request evidence is sufficient under ADR 0011 until the host
surfaces those fields.
